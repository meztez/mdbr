% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/advanced.R
\name{mdb_ddl}
\alias{mdb_ddl}
\title{Generate Schema Creation DDL}
\usage{
mdb_ddl(
  path,
  table = NULL,
  namespace = NULL,
  backend = c("access", "sybase", "oracle", "postgres", "mysql", "sqlite"),
  drop_table = FALSE,
  not_null = TRUE,
  default_values = FALSE,
  not_empty = FALSE,
  comments = TRUE,
  indexes = TRUE,
  relations = TRUE,
  as_list = TRUE
)
}
\arguments{
\item{path}{Path to \code{.mdb}/\code{.accdb} file.}

\item{table}{Single table option, equivalent to \code{-T/--table}. Default is to
export all user tables.}

\item{namespace}{Prefix identifiers with namespace, equivalent to
\code{-N/--namespace}.}

\item{backend}{Target DDL dialect. Supported values are \code{access}, \code{sybase},
\code{oracle}, \code{postgres}, \code{mysql}, and \code{sqlite}.}

\item{drop_table}{Issue \verb{DROP TABLE} statements.}

\item{not_null}{Include \verb{NOT NULL} constraints.}

\item{default_values}{Include \code{DEFAULT} values.}

\item{not_empty}{Include \verb{CHECK <> ''} constraints.}

\item{comments}{Include \verb{COMMENT ON} statements.}

\item{indexes}{Export indexes.}

\item{relations}{Request foreign key constraints. Current library-mode
implementation emits a placeholder comment; full FK export is not yet
implemented.}

\item{as_list}{Logical; defaults to \code{TRUE}. When \code{TRUE}, returns a named
\code{mdblist} of DDL text entries keyed by table name.}
}
\value{
When \code{as_list = TRUE}, a named \code{mdblist} of table-level DDL text.
If \code{table = NULL}, all user tables are included. When \code{as_list = FALSE},
a character scalar is returned for a single table or for \code{table = NULL}, and
a plain named list is returned for multiple tables.
}
\description{
\code{mdb-schema} (\code{mdb_ddl}) is a utility program distributed with MDB Tools.
It produces DDL (data definition language) output for the given database.
This can be passed to another database to create a replica of the original
Access table format.
}
\examples{
db <- mdbr:::.mdb_example_nwind_path()
if (nzchar(db)) {
  cat(mdb_schema(db, table = "Products", backend = "postgres", as_list = FALSE))
  ddl <- mdb_schema(db, table = c("Products", "Orders"), as_list = TRUE)
  ddl
}
}
